name: "Remote Relay Client"
version: "0.2.5"
slug: "remote_relay_client"
description: "Outbound-only secure relay client for Home Assistant API and WebSocket access"
arch:
  - amd64
  - aarch64

startup: services
boot: auto

host_network: false
homeassistant_api: true
auth_api: true

options:
  relay_url: ""
  device_id: ""
  secret: ""

schema:
  relay_url: str
  device_id: str
  secret: str
